interface:
  display_name: "Engineering Workflow"
  short_description: "Plan, build, and validate maintainable projects"
  default_prompt: "Use $engineering-workflow to clarify requirements, architecture, interfaces, configuration, and acceptance criteria, then implement and validate in stages."
policy:
  allow_implicit_invocation: true
